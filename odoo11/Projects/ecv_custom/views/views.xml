<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="ecv_custom.list">
      <field name="name">ecv_custom list</field>
      <field name="model">ecv_custom.ecv_custom</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="ecv_custom.action_window">
      <field name="name">ecv_custom window</field>
      <field name="res_model">ecv_custom.ecv_custom</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="ecv_custom.action_server">
      <field name="name">ecv_custom server</field>
      <field name="model_id" ref="model_ecv_custom_ecv_custom"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="ecv_custom" id="ecv_custom.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="ecv_custom.menu_1" parent="ecv_custom.menu_root"/>
    <menuitem name="Menu 2" id="ecv_custom.menu_2" parent="ecv_custom.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="ecv_custom.menu_1_list" parent="ecv_custom.menu_1"
              action="ecv_custom.action_window"/>
    <menuitem name="Server to list" id="ecv_custom" parent="ecv_custom.menu_2"
              action="ecv_custom.action_server"/>
    -->
  </data>
</odoo>